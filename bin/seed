#!/usr/bin/php
<?php
require '../src/helpers/helpers.php';
require "../vendor/autoload.php";
require '../config/config.php';
$seeders = require '../config/seeders.php';
foreach($seeders as $seeder){
    seed($seeder);
}